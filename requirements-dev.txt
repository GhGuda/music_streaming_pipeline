# requirements-dev.txt
# Runtime libs that source modules import (must match AWS runtimes!)
# Glue 4.0 = PySpark 3.3, Python 3.10. Glue 5.0 = PySpark 3.5, Python 3.11.
boto3>=1.34,<1.35
botocore>=1.34,<1.35
pandas>=2.2,<2.3
pyarrow>=14.0,<15.0
pyspark==3.5.4            # matches Glue 5.0

# Test
moto[s3,dynamodb,sqs,lambda]>=5.0,<6.0   # 5.x = @mock_aws API
pytest>=8.4,<9.0
pytest-cov>=5.0
pytest-mock>=3.14

# Lint / format / type / security
black>=25.0,<26.0
ruff>=0.12,<0.13
mypy>=1.10
pip-audit>=2.7

# Dev workflow
pre-commit>=3.7


# claude tool use -- platform.claude.com
