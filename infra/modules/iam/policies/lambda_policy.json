{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "S3ArchiveAccess",
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:PutObject",
        "s3:DeleteObject",
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::${raw_bucket_name}",
        "arn:aws:s3:::${raw_bucket_name}/*",
        "arn:aws:s3:::${archive_bucket}",
        "arn:aws:s3:::${archive_bucket}/*"
      ]
    },
    {
      "Sid": "KmsUsage",
      "Effect": "Allow",
      "Action": [
        "kms:Decrypt",
        "kms:Encrypt",
        "kms:GenerateDataKey"
      ],
      "Resource": [
        "${kms_key_arn}"
      ]
    },
    {
      "Sid": "DynamoDbKpiReadAccess",
      "Effect": "Allow",
      "Action": [
        "dynamodb:GetItem",
        "dynamodb:Query",
        "dynamodb:Scan"
      ],
      "Resource": [
        "${dynamodb_table_arn}",
        "${dynamodb_table_arn}/index/*"
      ]
    },
    {
      "Sid": "Logs",
      "Effect": "Allow",
      "Action": [
        "logs:CreateLogGroup",
        "logs:CreateLogStream",
        "logs:PutLogEvents"
      ],
      "Resource": "*"
    }
  ]
}
